#include <stdio.h>

int main() {
	//if() else
	//if() {} else
	//if() else {}
	//if() {} else {}
	//if() {} else if () {}
	for(int i = -1; i <= 1; i++) {
		if (i) printf("i is %d, and if executed\n",i);// if statement executes if the condition
				  // is not 0
	}
	
	return 0;
}
